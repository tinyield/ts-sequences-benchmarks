/**
 * Checks if a {number} is odd or not
 * @param {number} value the value to be checked
 * @returns {boolean} true if value is odd, false otherwise
 */
export function isOdd(value: number): boolean {
    return value % 2 !== 0;
}
